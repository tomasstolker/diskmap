language: python

dist: xenial

python:
  - 3.6
  - 3.7

install:
  - pip install -e .
  - pip install --upgrade -r requirements.txt
  - pip install coveralls

script:
  - pytest

after_success:
  - coveralls

notifications:
  - webhooks: https://coveralls.io/webhook
  - email: false
